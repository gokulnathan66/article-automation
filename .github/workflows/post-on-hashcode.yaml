name: Post on Hashnode

on:
  push:
    branches: [main]  # Trigger on pushes to 'main' branch
  workflow_dispatch:    # Allow manual triggers

jobs:
  post-on-hashnode:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code for access to README.md and script
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Load any previously saved repo variables into environment
      - name: Load saved post variables as env
        run: |
          echo "HASHNODE_SAVED_POST_ID=${{ vars.HASHNODE_SAVED_POST_ID }}" >> $GITHUB_ENV
          echo "HASHNODE_SAVED_POST_SLUG=${{ vars.HASHNODE_SAVED_POST_SLUG }}" >> $GITHUB_ENV
          echo "HASHNODE_SAVED_POST_TITLE=${{ vars.HASHNODE_SAVED_POST_TITLE }}" >> $GITHUB_ENV
          echo "HASHNODE_SAVED_POST_URL=${{ vars.HASHNODE_SAVED_POST_URL }}" >> $GITHUB_ENV
          echo "HASHNODE_SAVED_POST_PUBLISHED_AT=${{ vars.HASHNODE_SAVED_POST_PUBLISHED_AT }}" >> $GITHUB_ENV
          echo "HASHNODE_SAVED_POST_UPDATED_AT=${{ vars.HASHNODE_SAVED_POST_UPDATED_AT }}" >> $GITHUB_ENV

      # 3️⃣ Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 5️⃣ (Optional debug) List repo structure
      - name: Debug repo files
        run: |
          pwd
          ls -la
          ls -la api/ || echo "api directory not found"

      # 6️⃣ Run your post/update script, output JSON
      - name: Run Hashnode post script and capture output
        id: run_post
        run: |
          node api/hashnode.js > post_output.json

          echo "Script output:"
          cat post_output.json

          if jq empty post_output.json 2>/dev/null; then
            echo "JSON is valid, extracting fields..."
            echo "HASHNODE_SAVED_POST_ID=$(jq -r '.id' post_output.json)" >> $GITHUB_ENV
            echo "HASHNODE_SAVED_POST_SLUG=$(jq -r '.slug' post_output.json)" >> $GITHUB_ENV
            echo "HASHNODE_SAVED_POST_TITLE=$(jq -r '.title' post_output.json)" >> $GITHUB_ENV
            echo "HASHNODE_SAVED_POST_URL=$(jq -r '.url' post_output.json)" >> $GITHUB_ENV
            echo "HASHNODE_SAVED_POST_PUBLISHED_AT=$(jq -r '.published_at' post_output.json)" >> $GITHUB_ENV
            echo "HASHNODE_SAVED_POST_UPDATED_AT=$(jq -r '.updated_at' post_output.json)" >> $GITHUB_ENV
          else
            echo "❌ Error: Output is not valid JSON"
            cat post_output.json
            exit 1
          fi
        env:
          HASHNODE_PAT: ${{ secrets.HASHNODE_PAT }}
          HASHNODE_PUBLICATION_ID: ${{ secrets.HASHNODE_PUBLICATION_ID }}
          HASHNODE_PUBLICATION_HOST: ${{ secrets.HASHNODE_PUBLICATION_HOST }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
          GITHUB_BRANCH: ${{ github.ref_name }}

      # 7️⃣ Save details back into GitHub Repo Variables (Persistent between runs)
      - name: Save post details to repo variables
        run: |
          if jq -e 'has("error") | not' post_output.json; then
            echo "✅ Updating GitHub repository variables..."

            # Save all variables
            declare -A variables=(
              ["HASHNODE_SAVED_POST_ID"]=$(jq -r '.id' post_output.json)
              ["HASHNODE_SAVED_POST_SLUG"]=$(jq -r '.slug' post_output.json)
              ["HASHNODE_SAVED_POST_TITLE"]=$(jq -r '.title' post_output.json)
              ["HASHNODE_SAVED_POST_URL"]=$(jq -r '.url' post_output.json)
              ["HASHNODE_SAVED_POST_PUBLISHED_AT"]=$(jq -r '.published_at' post_output.json)
              ["HASHNODE_SAVED_POST_UPDATED_AT"]=$(jq -r '.updated_at' post_output.json)
            )

            for var in "${!variables[@]}"; do
              VALUE="${variables[$var]}"
              echo "Setting $var to: $VALUE"
              
              # Try to update existing variable, create if it doesn't exist
              gh api --method PATCH /repos/${{ github.repository }}/actions/variables/$var \
                -f name="$var" \
                -f value="$VALUE" 2>/dev/null || \
              gh api --method POST /repos/${{ github.repository }}/actions/variables \
                -f name="$var" \
                -f value="$VALUE"
            done
          else
            echo "⏭ Skipped saving repo variables due to errors."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.VAR_EDIT_TOKEN_GIT }}

      # 8️⃣ Final output
      - name: Show post info
        run: |
          echo "📢 Article posted/updated on Hashnode."
          echo "ID: $HASHNODE_SAVED_POST_ID"
          echo "Slug: $HASHNODE_SAVED_POST_SLUG"  
          echo "Title: $HASHNODE_SAVED_POST_TITLE"
          echo "URL: $HASHNODE_SAVED_POST_URL"
          echo "Published At: $HASHNODE_SAVED_POST_PUBLISHED_AT"
          echo "Updated At: $HASHNODE_SAVED_POST_UPDATED_AT"
